# Setup choices on the test laptop

Two original screenshots from the installed wizard in Tokyo Night, using a manual dry-run and the invented name Dot. Gum is version 2.0.0. Apply was never selected; the preview was closed through its Leave confirmation. The owned terminal was verified absent, its unit collected, and the original theme and watched Quickshell session remained in place.

- [Age choice](age.png): the cursor starts on Ages 3–5, while the wizard specifies 6–8 as its default. The comma in the rendered 6–8 option interferes with the chooser's selected-value parsing.
- [Final confirmation](summary.png): only Ready?, Apply, and Change something remain visible. The chooser redraw clears the settings summary before the parent can review it.

The driver and an independent reviewer inspected both originals and traced each to current source. The installed wizard-screens file matches main 441ab32; the installed shared renderer is older, with the same causal selection and redraw behavior. These are pre-fix observations, not setup or account-creation acceptance.

[SHA256SUMS](SHA256SUMS) records the unchanged originals.
