# Wizard summary candidate — Latte preview

Actual installed VM captures at 1280×800, source `e9384a7a3464c99fb2dc501d6a24abd68de83df5`, package SHA-256 `e87d80f2ade3e9010d73c8c51387848f48f3356356205c2b20ef1b157abf41a3`.

The settled Ready card shows the selected settings. Changing Desktop to Level 2 recomputes the card; Esc returns to the password screen, and re-entering the invented password preserves Level 2 (custom). Root and an independent session inspected these frames. Ben is an invented fixture; this is a dry-run preview with no Apply action.

The Advanced checklist frame records a separate defect: the Starter apps row clips at the right edge. These are exact original screenshots; SHA256SUMS records their hashes.

This gallery covers the Latte frames only. Tokyo Night, final restoration, and the separate installed readback remain pending. It is not a completed release walkthrough or a claim that #167 is done.
