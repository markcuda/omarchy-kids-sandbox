# Wizard summary candidate — installed preview

Actual VM captures at 1280×800, source `e9384a7a3464c99fb2dc501d6a24abd68de83df5`, package SHA-256 `e87d80f2ade3e9010d73c8c51387848f48f3356356205c2b20ef1b157abf41a3`.

The settled Ready card shows the selected settings. In fresh Catppuccin Latte and Tokyo Night sessions, changing Desktop to Level 2 recomputes the card; Esc returns to the password screen, and re-entering the invented password preserves Level 2 (custom). Root and an independent session inspected these frames, including the Leave confirmation in both themes. Ben is invented; this was a dry-run preview with no Apply action or account creation. Quickshell watching was enabled for each session.

Both previews closed normally. The original light theme and greeter were restored; owned wizard and watcher processes were gone. A separate installed readback passed package integrity (181 files, zero altered), pinned runtime bytes and metadata, recorded child settings, themes, timer, and greeter-only state.

The Advanced checklist frames record a separate defect: the Starter apps row clips at the right edge ([#172](https://github.com/markcuda/omarchy-kids-sandbox/issues/172)). These are exact original screenshots; SHA256SUMS records their hashes.

The Mac initially rejected one Tokyo screenshot copy for lack of space; the saved original was recovered from the laptop before any further input. Early Latte Ready/Advanced captures caught partial redraws; the gallery uses separately inspected settled frames. The input guard also stopped when the Latte screensaver took focus and resumed only after the wizard was verified again.

This completes the named installed candidate scenario `wizard-summary-visible-167`. It is not a full release walkthrough or post-merge validation.
